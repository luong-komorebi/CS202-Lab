#include "CItem.h"
#include <vector>
using namespace std;

//CItem* void::setHidden(bool a, bool b) {
//  this->hiddenOrNot = a;
//  if (this->getType() == "Folder") {
//    return setHidden(a, b);
//  }
//  return this;
//}
